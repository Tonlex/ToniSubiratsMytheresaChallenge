{
  "name": "cypress-fashionhub",
  "version": "1.0.0",
  "description": "Mytheresa automation challenge",
  "main": "index.js",
  "scripts": {
      "cy:open:local": "cypress open --env ENV=local",
      "cy:run:local": "cypress run --env ENV=local",
      "cy:open:staging": "cypress open --env ENV=staging",
      "cy:run:staging": "cypress run --env ENV=staging",
      "cy:open:production": "cypress open --env ENV=production",
      "cy:run:production": "cypress run --env ENV=production"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.15.1"
  }
}
